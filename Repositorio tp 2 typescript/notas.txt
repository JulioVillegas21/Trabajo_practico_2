✅ Paso 2: Instalá prompt-sync
npm install prompt-sync

npm init -y

✅ Paso 3: Verificá que está instalado
npm ls prompt-sync

✅ Paso 4: Instalá los tipos (solo si usás TypeScript)
npm install --save-dev @types/prompt-sync

✅tsconfig = npx tsc --init



{
  "compilerOptions": {
    "rootDir": "./src",                  // 📂 Carpeta con tu código fuente
    "outDir": "./dist",                  // 📁 Carpeta donde se genera el JS compilado
    "module": "ESNext",                  // 📦 Usa módulos ES (import/export)
    "target": "ES2020",                  // 🎯 Código JS moderno (ES2020)
    "lib": ["ESNext"],                   // 📚 Librerías JS modernas
    "moduleResolution": "node",          // 🔍 Resuelve módulos al estilo Node.js
    "esModuleInterop": true,             // ⚙️ Mejor compatibilidad con módulos comunes
    "allowSyntheticDefaultImports": true,// 🛠️ Permite importar módulos sin default export
    "strict": true,                      // ✅ Chequeos estrictos para menos errores
    "skipLibCheck": true,                // ⏭️ Ignora chequeo en librerías para compilar más rápido
    "sourceMap": true,                   // 🗺️ Genera mapas para debuggear el JS compilado
    "declaration": true                 // 📄 Genera archivos .d.ts con tipos
  },
  "include": ["src/**/*"]                // 📂 Incluir todos los archivos TS dentro de src/
}


hay que agregar las lineas en ts confing 

"compilerOptions": {
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true}


    ya que si no no puedo importar el prompt